{"metadata":{"kernelspec":{"name":"python3","display_name":"Python 3","language":"python"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[],"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"markdown","source":"# QR-ViT-Det: All-in-One Notebook\n\nA single Jupyter notebook that includes the entire codebase (model, dataset, training, evaluation, and export) in executable cells. Edit the configuration in one place and run end-to-end training, evaluation, and inference without any external files.","metadata":{}},{"cell_type":"code","source":"","metadata":{"trusted":true},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# Basic imports and utilities\nimport math\nimport time\nimport random\nfrom pathlib import Path\nimport json\n\nimport numpy as np\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nfrom PIL import Image\n\n# Optional: for dataset generation / augmentation helpers\ntry:\n import albumentations as A\n import cv2\nexcept Exception:\n A = None # Will be needed if dataset generation is used\n cv2 = None\n\n# Set a deterministic seed for reproducibility during notebook runs\ndef set_seed(s: int = 42):\n random.seed(s)\n np.random.seed(s)\n torch.manual_seed(s)\n if torch.cuda.is_available(): torch.cuda.manual_seed_all(s)\n\nset_seed(1234)\nprint('Notebook setup complete.')","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.255791Z","iopub.execute_input":"2026-04-11T07:06:41.256622Z","iopub.status.idle":"2026-04-11T07:06:41.264885Z","shell.execute_reply.started":"2026-04-11T07:06:41.256587Z","shell.execute_reply":"2026-04-11T07:06:41.264255Z"}},"outputs":[{"name":"stdout","text":"Notebook setup complete.\n","output_type":"stream"}],"execution_count":11},{"cell_type":"code","source":"# 1) Configuration (CFG) block\nfrom dataclasses import dataclass, field\nfrom typing import Optional\n\n@dataclass\nclass Config:\n # Image/frame settings\n img_w: int = 320\n img_h: int = 180\n in_channels: int = 1 # Grayscale input\n\n # ViT-like backbone (CNN stem + Transformer)\n patch_stride: int = 10 # CNN stem reduces by stride 10\n embed_dim: int = 128\n num_heads: int = 8\n enc_layers: int = 4\n dec_layers: int = 4\n mlp_ratio: float = 4.0\n dropout: float = 0.15\n\n # Detection head / queries\n num_queries: int = 20\n num_queries_rows: int = 4\n num_queries_cols: int = 5\n\n # Loss weights (Hungarian matching)\n cost_class: float = 2.0\n cost_bbox: float = 5.0\n cost_giou: float = 2.0\n loss_class: float = 2.0\n loss_bbox: float = 5.0\n loss_giou: float = 2.0\n eos_coef: float = 0.5\n\n # Dataset / training\n dataset_dir: Path = Path(\"data/qr_dataset\")\n num_train: int = 4000\n num_val: int = 1000\n num_test: int = 600\n max_qr_per_image: int = 4\n background_dir: Path = Path(\"data/backgrounds\")\n\n # Training hyperparameters\n batch_size: int = 50\n num_workers: int = 2\n epochs: int = 100\n lr: float = 2e-4\n lr_backbone: float = 2e-4\n weight_decay: float = 1e-5\n lr_drop: int = 70\n grad_clip: float = 50.0\n checkpoint_dir: Path = Path(\"checkpoints\")\n log_interval: int = 10\n\n # Inference / evaluation\n conf_threshold: float = 0.4\n iou_threshold: float = 0.5\n\n# Instantiate CFG with defaults\nCFG = Config()\nprint('CFG created with values: img_w={}, img_h={}, embed_dim={}'.format(CFG.img_w, CFG.img_h, CFG.embed_dim))","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.266107Z","iopub.execute_input":"2026-04-11T07:06:41.266485Z","iopub.status.idle":"2026-04-11T07:06:41.284149Z","shell.execute_reply.started":"2026-04-11T07:06:41.266459Z","shell.execute_reply":"2026-04-11T07:06:41.283250Z"}},"outputs":[{"name":"stdout","text":"CFG created with values: img_w=320, img_h=180, embed_dim=128\n","output_type":"stream"}],"execution_count":12},{"cell_type":"code","source":"# 2) Model components (QR-ViT-Det) copied in, no external file imports\nimport math\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass CNNStem(nn.Module):\n    def __init__(self, in_channels: int = 1, embed_dim: int = 128):\n        super().__init__()\n        self.body = nn.Sequential(\n            nn.Conv2d(in_channels, 32, kernel_size=4, stride=2, padding=1, bias=False),\n            nn.BatchNorm2d(32),\n            nn.GELU(),\n            nn.Conv2d(32, 64, kernel_size=5, stride=5, padding=0, bias=False),\n            nn.BatchNorm2d(64),\n            nn.GELU(),\n            nn.Conv2d(64, embed_dim, kernel_size=3, stride=1, padding=1, bias=False),\n            nn.BatchNorm2d(embed_dim),\n            nn.GELU()\n        )\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        return self.body(x)\n\nclass PositionalEncoding2D(nn.Module):\n    def __init__(self, embed_dim: int, h_feat: int, w_feat: int):\n        super().__init__()\n        assert embed_dim % 4 == 0, 'embed_dim must be divisible by 4'\n        d = embed_dim // 2\n        div_term = torch.exp(torch.arange(0, d, 2).float() * (-math.log(10000.0) / d))\n        pe = torch.zeros(h_feat, w_feat, embed_dim)\n        y_pos = torch.arange(h_feat).unsqueeze(1).float()\n        x_pos = torch.arange(w_feat).unsqueeze(1).float()\n        pe[:, :, 0:d:2] = (torch.sin(y_pos * div_term)).unsqueeze(1).expand(h_feat, w_feat, -1)\n        pe[:, :, 1:d:2] = (torch.cos(y_pos * div_term)).unsqueeze(1).expand(h_feat, w_feat, -1)\n        pe[:, :, d::2] = (torch.sin(x_pos * div_term)).unsqueeze(0).expand(h_feat, w_feat, -1)\n        pe[:, :, d + 1 :: 2] = (torch.cos(x_pos * div_term)).unsqueeze(0).expand(h_feat, w_feat, -1)\n        pe = pe.view(1, h_feat * w_feat, embed_dim)\n        self.register_buffer('pe', pe)\n\n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        return x + self.pe\n\nclass TransformerEncoderLayer(nn.Module):\n    def __init__(self, embed_dim: int, num_heads: int, mlp_ratio: float, dropout: float):\n        super().__init__()\n        self.self_attn = nn.MultiheadAttention(embed_dim, num_heads, dropout=dropout, batch_first=True)\n        hidden = int(embed_dim * mlp_ratio)\n        self.ffn = nn.Sequential(\n            nn.Linear(embed_dim, hidden),\n            nn.GELU(),\n            nn.Dropout(dropout),\n            nn.Linear(hidden, embed_dim),\n            nn.Dropout(dropout),\n        )\n        self.norm1 = nn.LayerNorm(embed_dim)\n        self.norm2 = nn.LayerNorm(embed_dim)\n        self.drop = nn.Dropout(dropout)\n\n    def forward(self, x: torch.Tensor, pos: torch.Tensor) -> torch.Tensor:\n        h = self.norm1(x)\n        q = k = h + pos\n        attn_out, _ = self.self_attn(q, k, h)\n        x = x + self.drop(attn_out)\n        x = x + self.ffn(self.norm2(x))\n        return x\n\nclass TransformerDecoderLayer(nn.Module):\n    def __init__(self, embed_dim: int, num_heads: int, mlp_ratio: float, dropout: float):\n        super().__init__()\n        self.self_attn = nn.MultiheadAttention(embed_dim, num_heads, dropout=dropout, batch_first=True)\n        self.cross_attn = nn.MultiheadAttention(embed_dim, num_heads, dropout=dropout, batch_first=True)\n        hidden = int(embed_dim * mlp_ratio)\n        self.ffn = nn.Sequential(\n            nn.Linear(embed_dim, hidden),\n            nn.GELU(),\n            nn.Dropout(dropout),\n            nn.Linear(hidden, embed_dim),\n            nn.Dropout(dropout),\n        )\n        self.norm1 = nn.LayerNorm(embed_dim)\n        self.norm2 = nn.LayerNorm(embed_dim)\n        self.norm3 = nn.LayerNorm(embed_dim)\n        self.drop = nn.Dropout(dropout)\n\n    def forward(self, tgt: torch.Tensor, memory: torch.Tensor, query_pos: torch.Tensor, pos: torch.Tensor) -> torch.Tensor:\n        h = self.norm1(tgt)\n        q = k = h + query_pos\n        sa, _ = self.self_attn(q, k, h)\n        tgt = tgt + self.drop(sa)\n        h = self.norm2(tgt)\n        q = h + query_pos\n        k = memory + pos\n        ca, _ = self.cross_attn(q, k, memory)\n        tgt = tgt + self.drop(ca)\n        tgt = tgt + self.ffn(self.norm3(tgt))\n        return tgt\n\nclass MLP(nn.Module):\n    \"\"\"Simple 3-layer MLP used as the detection head.\"\"\"\n    def __init__(self, in_dim: int, hidden_dim: int, out_dim: int, num_layers: int = 3):\n        super().__init__()\n        dims = [in_dim] + [hidden_dim] * (num_layers - 1) + [out_dim]\n        self.layers = nn.ModuleList([nn.Linear(dims[i], dims[i + 1]) for i in range(len(dims) - 1)])\n    \n    def forward(self, x: torch.Tensor) -> torch.Tensor:\n        for i, layer in enumerate(self.layers):\n            x = F.relu(layer(x)) if i < len(self.layers) - 1 else layer(x)\n        return x\n\nclass QRViTDet(nn.Module):\n    def __init__(self, cfg=CFG):\n        super().__init__()\n        self.cfg = cfg\n        \n        h_feat = cfg.img_h // cfg.patch_stride\n        w_feat = cfg.img_w // cfg.patch_stride\n        \n        # Backbone / CNN stem\n        self.backbone = CNNStem(cfg.in_channels, cfg.embed_dim)\n        \n        # Positional encoding\n        self.pos_enc = PositionalEncoding2D(cfg.embed_dim, h_feat, w_feat)\n        \n        # Encoder\n        self.encoder = nn.ModuleList(\n            [\n                TransformerEncoderLayer(cfg.embed_dim, cfg.num_heads, cfg.mlp_ratio, cfg.dropout)\n                for _ in range(cfg.enc_layers)\n            ]\n        )\n        \n        self.enc_norm = nn.LayerNorm(cfg.embed_dim)\n        \n        # Anchors / explicit query grid\n        y = (torch.arange(cfg.num_queries_rows).float() + 0.5) / cfg.num_queries_rows\n        x = (torch.arange(cfg.num_queries_cols).float() + 0.5) / cfg.num_queries_cols\n        grid_y, grid_x = torch.meshgrid(y, x, indexing='ij')\n        \n        anchors = torch.stack([grid_x.flatten(), grid_y.flatten()], dim=-1)\n        self.register_buffer('anchors', anchors)\n        \n        self.query_pos_proj = nn.Sequential(\n            nn.Linear(2, cfg.embed_dim),\n            nn.ReLU(),\n            nn.Linear(cfg.embed_dim, cfg.embed_dim)\n        )\n        \n        # Decoder\n        self.decoder = nn.ModuleList(\n            [\n                TransformerDecoderLayer(cfg.embed_dim, cfg.num_heads, cfg.mlp_ratio, cfg.dropout) \n                for _ in range(cfg.dec_layers)\n            ]\n        )\n        self.dec_norm = nn.LayerNorm(cfg.embed_dim)\n        \n        # Heads\n        self.bbox_head = MLP(cfg.embed_dim, cfg.embed_dim, 4, num_layers=3)\n        self.class_head = nn.Linear(cfg.embed_dim, 2)\n        \n        self.init_weights()\n\n    def init_weights(self):\n        for m in self.modules():\n            if isinstance(m, nn.Linear):\n                nn.init.xavier_uniform_(m.weight)\n                if m.bias is not None:\n                    nn.init.zeros_(m.bias)\n                elif isinstance(m, (nn.BatchNorm2d, nn.LayerNorm)):\n                    nn.init.ones_(m.weight)\n                    nn.init.zeros_(m.bias)\n                elif isinstance(m, nn.Conv2d):\n                    nn.init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')\n\n    def forward(self, x: torch.Tensor) -> dict:\n        B = x.size(0)\n        \n        feat = self.backbone(x)\n        E = feat.size(1)\n        feat_seq = feat.flatten(2).permute(0, 2, 1)\n        pos = self.pos_enc.pe\n        \n        memory = feat_seq\n        for layer in self.encoder:\n            memory = layer(memory, pos=pos)\n        memory = self.enc_norm(memory)\n        \n        query_pos = self.query_pos_proj(self.anchors)\n        query_pos = query_pos.unsqueeze(0).expand(B, -1, -1)\n        tgt = torch.zeros_like(query_pos)\n        \n        for layer in self.decoder:\n            tgt = layer(tgt, memory, query_pos=query_pos, pos=pos)\n        tgt = self.dec_norm(tgt)\n        \n        tgt_out = tgt + query_pos\n        raw_preds = self.bbox_head(tgt_out)\n        delta_cxcy = raw_preds[..., :2]\n        raw_wh = raw_preds[..., 2:]\n        \n        ref_points = self.anchors.unsqueeze(0).expand(B, -1, -1)\n        cxcy = (ref_points + delta_cxcy.tanh() * 0.5).clamp(min=0.0, max=1.0)\n        w_h = raw_wh.sigmoid()\n        cx, cy = cxcy.unbind(-1)\n        w, h = w_h.unbind(-1)\n        x1 = cx - 0.5 * w\n        y1 = cy - 0.5 * h\n        x2 = cx + 0.5 * w\n        y2 = cy + 0.5 * h\n        pred_boxes = torch.stack([x1, y1, x2, y2], dim=-1)\n        pred_logits = self.class_head(tgt_out)\n        return {\"pred_boxes\": pred_boxes, \"pred_logits\": pred_logits}\n\n    @property\n    def backbone_params(self):\n        return list(self.backbone.parameters())\n    \n    @property\n    def non_backbone_params(self):\n        backbone_ids = {id(p) for p in self.backbone.parameters()}\n        return [p for p in self.parameters() if id(p) not in backbone_ids]\n\nprint('QRViTDet module defined.')","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.293794Z","iopub.execute_input":"2026-04-11T07:06:41.294413Z","iopub.status.idle":"2026-04-11T07:06:41.327081Z","shell.execute_reply.started":"2026-04-11T07:06:41.294379Z","shell.execute_reply":"2026-04-11T07:06:41.326242Z"}},"outputs":[{"name":"stdout","text":"QRViTDet module defined.\n","output_type":"stream"}],"execution_count":13},{"cell_type":"code","source":"# dataset_gen.py\n# ─────────────────────────────────────────────────────────────────────────────\n# Tools for:\n#   1. Generating synthetic QR-code scenes (backgrounds + placed QR codes)\n#   2. Auto-labelling corners (top-left, bottom-right) in normalized coords\n#   3. Augmenting existing images with albumentations\n#\n# Usage:\n#   python dataset_gen.py --num_train 4000 --num_val 500 --num_test 200\n# ─────────────────────────────────────────────────────────────────────────────\n\nimport argparse\nimport json\nimport math\nimport random\nimport string\nimport uuid\nfrom pathlib import Path\nfrom typing import List, Tuple, Dict, Any\n\nimport numpy as np\ntry: \n    import qrcode\nexcept:\n    !pip install -U qrcode\n    import qrcode\nfrom PIL import Image, ImageDraw, ImageFilter, ImageEnhance\nimport albumentations as A\nfrom albumentations.core.transforms_interface import DualTransform\nimport cv2\n\n\n# ──────────────────────────────────────────────────────────────────────────────\n# 1.  BACKGROUND GENERATORS\n# ──────────────────────────────────────────────────────────────────────────────\n\n\ndef _bg_solid(w: int, h: int) -> np.ndarray:\n    \"\"\"Uniform random-grey background.\"\"\"\n    val = random.randint(50, 230)\n    return np.full((h, w), val, dtype=np.uint8)\n\n\ndef _bg_gradient(w: int, h: int) -> np.ndarray:\n    \"\"\"Random linear gradient background.\"\"\"\n    a, b = random.randint(30, 220), random.randint(30, 220)\n    col = np.linspace(a, b, w, dtype=np.float32)\n    row = np.linspace(random.randint(30, 220), random.randint(30, 220), h, dtype=np.float32)\n    bg = (col[np.newaxis, :] * 0.5 + row[:, np.newaxis] * 0.5).clip(0, 255)\n    return bg.astype(np.uint8)\n\n\ndef _bg_noise(w: int, h: int) -> np.ndarray:\n    \"\"\"Gaussian noise background.\"\"\"\n    mean = random.randint(80, 180)\n    bg = np.random.normal(mean, random.randint(10, 40), (h, w)).clip(0, 255)\n    return bg.astype(np.uint8)\n\n\ndef _bg_perlin_like(w: int, h: int) -> np.ndarray:\n    \"\"\"Cheap multi-scale noise approximation using resized blur.\"\"\"\n    base = np.random.randint(0, 256, (h // 8, w // 8), dtype=np.uint8)\n    pil = Image.fromarray(base).resize((w, h), Image.BILINEAR)\n    pil = pil.filter(ImageFilter.GaussianBlur(radius=random.randint(2, 8)))\n    return np.array(pil)\n\n\ndef _bg_checkerboard(w: int, h: int) -> np.ndarray:\n    \"\"\"Random-sized checkerboard pattern.\"\"\"\n    cell = random.randint(20, 80)\n    xs = np.arange(w) // cell\n    ys = np.arange(h) // cell\n    grid = (xs[np.newaxis, :] + ys[:, np.newaxis]) % 2\n    c1, c2 = random.randint(20, 100), random.randint(150, 240)\n    return np.where(grid == 0, c1, c2).astype(np.uint8)\n\n\ndef _bg_stripes(w: int, h: int) -> np.ndarray:\n    \"\"\"Horizontal or vertical stripes.\"\"\"\n    n = random.randint(5, 30)\n    colors = np.random.randint(20, 240, n, dtype=np.uint8)\n    if random.random() < 0.5:\n        # Horizontal stripes\n        indices = np.arange(h) * n // h\n        return colors[indices % n].reshape(h, 1).repeat(w, axis=1)\n    else:\n        # Vertical stripes\n        indices = np.arange(w) * n // w\n        return colors[indices % n].reshape(1, w).repeat(h, axis=0)\n\n\ndef _bg_from_file(w: int, h: int, path: Path) -> np.ndarray:\n    \"\"\"Load a real image background, convert to grayscale.\"\"\"\n    img = Image.open(path).convert(\"L\").resize((w, h), Image.BILINEAR)\n    return np.array(img)\n\n\n_BG_GENERATORS = [\n    _bg_solid,\n    _bg_gradient,\n    _bg_noise,\n    _bg_perlin_like,\n    _bg_checkerboard,\n    _bg_stripes,\n]\n\n\ndef make_background(w: int, h: int, bg_dir: Path = None) -> np.ndarray:\n    \"\"\"Return a (H, W) uint8 grayscale background.\"\"\"\n    # 30% chance to use a real background if the folder exists and has images\n    if bg_dir and bg_dir.is_dir():\n        files = list(bg_dir.glob(\"*.jpg\")) + list(bg_dir.glob(\"*.png\"))\n        if files and random.random() < 0.30:\n            return _bg_from_file(w, h, random.choice(files))\n    return random.choice(_BG_GENERATORS)(w, h)\n\n\n# ──────────────────────────────────────────────────────────────────────────────\n# 2.  QR CODE GENERATOR\n# ──────────────────────────────────────────────────────────────────────────────\n\n\ndef _random_data() -> str:\n    \"\"\"Generate a realistic random QR payload.\"\"\"\n    kind = random.random()\n    if kind < 0.3:\n        return \"https://\" + \"\".join(random.choices(string.ascii_lowercase, k=8)) + \".com\"\n    elif kind < 0.6:\n        return \"\".join(\n            random.choices(string.ascii_letters + string.digits, k=random.randint(8, 40))\n        )\n    else:\n        return str(random.randint(100000, 999999999))\n\n\ndef generate_qr_pil(size_px: int) -> Image.Image:\n    \"\"\"\n    Create a square QR code PIL image of exactly `size_px × size_px`.\n    Returns a grayscale (L-mode) image.\n    \"\"\"\n    data = _random_data()\n    version = random.choice([1, 2, 3, 4, 5])\n    box_size = max(1, size_px // (21 + (version - 1) * 4 + 8))  # rough estimate\n    qr = qrcode.QRCode(\n        version=version,\n        error_correction=random.choice(\n            [\n                qrcode.constants.ERROR_CORRECT_L,\n                qrcode.constants.ERROR_CORRECT_M,\n                qrcode.constants.ERROR_CORRECT_H,\n            ]\n        ),\n        box_size=box_size,\n        border=random.randint(1, 4),\n    )\n    qr.add_data(data)\n    qr.make(fit=True)\n\n    # Random inverted QR codes\n    fill_color = random.randint(0, 60)\n    back_color = random.randint(200, 255)\n    if random.random() < 0.1:\n        fill_color, back_color = back_color, fill_color\n\n    img = qr.make_image(fill_color=fill_color, back_color=back_color)\n    img = img.convert(\"L\").resize((size_px, size_px), Image.NEAREST)\n    return img\n\n\n# ──────────────────────────────────────────────────────────────────────────────\n# 3.  SCENE COMPOSER\n# ──────────────────────────────────────────────────────────────────────────────\n\n\ndef _apply_perspective(qr_arr: np.ndarray, max_skew: float = 0.15) -> Tuple[np.ndarray, np.ndarray]:\n    \"\"\"\n    Apply a mild random perspective warp to a square QR code array.\n    Returns (warped_img, src_corners) where src_corners = [[x,y], ...] 4 points.\n    \"\"\"\n    h, w = qr_arr.shape\n    pts_src = np.float32([[0, 0], [w, 0], [w, h], [0, h]])\n    jitter = max_skew * w\n    pts_dst = pts_src + np.random.uniform(-jitter, jitter, pts_src.shape).astype(np.float32)\n    # Keep the order (TL, TR, BR, BL) intact\n    M = cv2.getPerspectiveTransform(pts_src, pts_dst)\n    warped = cv2.warpPerspective(\n        qr_arr, M, (w, h), flags=cv2.INTER_LINEAR, borderMode=cv2.BORDER_REPLICATE\n    )\n    return warped, pts_dst\n\n\ndef _apply_shear(qr_arr: np.ndarray, max_shear: float = 0.2) -> np.ndarray:\n    \"\"\"Apply random shear to QR array.\"\"\"\n    h, w = qr_arr.shape\n    shear = random.uniform(-max_shear, max_shear)\n    M = np.float32([[1, shear, 0], [0, 1, 0]])\n    sheared = cv2.warpAffine(\n        qr_arr, M, (w, h), flags=cv2.INTER_LINEAR, borderMode=cv2.BORDER_REPLICATE\n    )\n    return sheared\n\n\ndef compose_scene(\n    img_w: int = CFG.img_w,\n    img_h: int = CFG.img_h,\n    n_qr: int = None,\n    bg_dir: Path = None,\n) -> Tuple[np.ndarray, List[Dict]]:\n    \"\"\"\n    Build a 1-channel (H, W) uint8 scene with 1..max_qr_per_image QR codes.\n\n    Returns\n    -------\n    scene : np.ndarray  shape (H, W) uint8\n    labels : List[dict]\n        Each dict: {\"x1\": float, \"y1\": float, \"x2\": float, \"y2\": float}\n        All coords normalised to [0, 1].\n    \"\"\"\n    n_qr = n_qr or random.randint(1, CFG.max_qr_per_image)\n    bg = make_background(img_w, img_h, bg_dir).astype(np.float32)\n    labels = []\n    occupied = []  # list of (x1,y1,x2,y2) pixel rects, for overlap avoidance\n\n    for _ in range(n_qr):\n        # Random QR size: 5%–35% of the shorter image dimension\n        short = min(img_w, img_h)\n        qr_size = random.randint(int(short * 0.05), int(short * 0.35))\n        qr_img = generate_qr_pil(qr_size)\n        qr_arr = np.array(qr_img, dtype=np.float32)\n\n        # Optional photometric distortions on QR\n        qr_arr_uint8 = qr_arr.clip(0, 255).astype(np.uint8)\n        pil_qr = Image.fromarray(qr_arr_uint8)\n        if random.random() < 0.3:\n            gamma = random.uniform(0.5, 2.0)\n            gamma_table = np.array([((i / 255.0) ** (1 / gamma)) * 255 for i in range(256)]).astype(\n                np.uint8\n            )\n            pil_qr = Image.fromarray(gamma_table[pil_qr])\n        if random.random() < 0.2:\n            pil_qr = pil_qr.filter(ImageFilter.UnsharpMask(radius=1, percent=150, threshold=3))\n        if random.random() < 0.2:\n            radius = random.uniform(0.5, 2.5)\n            pil_qr = pil_qr.filter(ImageFilter.GaussianBlur(radius=radius))\n        if random.random() < 0.1:\n            pil_qr = pil_qr.filter(ImageFilter.MedianFilter(size=3))\n        qr_arr = np.array(pil_qr, dtype=np.float32)\n\n        # Optional mild perspective warp\n        if random.random() < 0.4:\n            qr_arr, _ = _apply_perspective(qr_arr, max_skew=0.08)\n\n        # Optional rotation (±15°)\n        if random.random() < 0.3:\n            angle = random.uniform(-15, 15)\n            pil_tmp = Image.fromarray(qr_arr.astype(np.uint8)).rotate(\n                angle, expand=True, fillcolor=int(qr_arr.mean())\n            )\n            qr_arr = np.array(pil_tmp, dtype=np.float32)\n\n        # Optional shear\n        if random.random() < 0.3:\n            qr_arr = _apply_shear(qr_arr)\n\n        qh, qw = qr_arr.shape\n\n        # Try to place without excessive overlap (max 5 attempts)\n        placed = False\n        for attempt in range(5):\n            x1 = random.randint(0, max(0, img_w - qw - 1))\n            y1 = random.randint(0, max(0, img_h - qh - 1))\n            x2, y2 = x1 + qw, y1 + qh\n            overlap = False\n            for ox1, oy1, ox2, oy2 in occupied:\n                ix1, iy1 = max(x1, ox1), max(y1, oy1)\n                ix2, iy2 = min(x2, ox2), min(y2, oy2)\n                inter = max(0, ix2 - ix1) * max(0, iy2 - iy1)\n                union = (x2 - x1) * (y2 - y1) + (ox2 - ox1) * (oy2 - oy1) - inter\n                if union > 0 and inter / union > 0.1:\n                    overlap = True\n                    break\n            if not overlap or attempt == 4:\n                placed = True\n                break\n\n        if not placed:\n            continue\n\n        # Clip to image boundary\n        x2c, y2c = min(x2, img_w), min(y2, img_h)\n        qr_crop = qr_arr[: y2c - y1, : x2c - x1]\n\n        # Blend onto background (alpha = random opacity for realism)\n        alpha = random.uniform(0.75, 1.0)\n        bg[y1:y2c, x1:x2c] = alpha * qr_crop + (1 - alpha) * bg[y1:y2c, x1:x2c]\n        occupied.append((x1, y1, x2c, y2c))\n\n        labels.append(\n            {\n                \"x1\": x1 / img_w,\n                \"y1\": y1 / img_h,\n                \"x2\": x2c / img_w,\n                \"y2\": y2c / img_h,\n            }\n        )\n\n    # Global photometric augmentation on the full scene\n    scene = bg.clip(0, 255).astype(np.uint8)\n    pil_scene = Image.fromarray(scene)\n\n    # Brightness / contrast jitter\n    pil_scene = ImageEnhance.Brightness(pil_scene).enhance(random.uniform(0.5, 1.5))\n    pil_scene = ImageEnhance.Contrast(pil_scene).enhance(random.uniform(0.6, 1.6))\n\n    # Random gamma\n    if random.random() < 0.3:\n        gamma = random.uniform(0.7, 1.3)\n        scene_np = np.array(pil_scene, dtype=np.float32) / 255.0\n        scene_np = scene_np**gamma\n        scene_np = (scene_np * 255).clip(0, 255).astype(np.uint8)\n        pil_scene = Image.fromarray(scene_np)\n\n    # Random compression\n    if random.random() < 0.3:\n        quality = random.randint(50, 95)\n        scene_np = np.array(pil_scene)\n        encode_param = [int(cv2.IMWRITE_JPEG_QUALITY), quality]\n        _, encimg = cv2.imencode(\".jpg\", scene_np, encode_param)\n        scene_np = cv2.imdecode(encimg, cv2.IMREAD_GRAYSCALE)\n        pil_scene = Image.fromarray(scene_np)\n\n    # Random motion blur\n    if random.random() < 0.2:\n        scene_np = np.array(pil_scene)\n        kernel_size = random.randint(3, 10)\n        kernel = np.zeros((kernel_size, kernel_size))\n        if random.random() < 0.5:\n            kernel[kernel_size // 2, :] = 1.0 / kernel_size  # horizontal\n        else:\n            kernel[:, kernel_size // 2] = 1.0 / kernel_size  # vertical\n        scene_np = cv2.filter2D(scene_np, -1, kernel)\n        pil_scene = Image.fromarray(scene_np)\n\n    # Random Gaussian blur (simulate out-of-focus)\n    if random.random() < 0.3:\n        pil_scene = pil_scene.filter(ImageFilter.GaussianBlur(radius=random.uniform(0.5, 2.5)))\n\n    # Additive noise\n    scene_np = np.array(pil_scene, dtype=np.float32)\n    scene_np += np.random.normal(0, random.uniform(0, 15), scene_np.shape)\n    scene = scene_np.clip(0, 255).astype(np.uint8)\n\n    # Add salt and pepper noise\n    if random.random() < 0.2:\n        prob = random.uniform(0.001, 0.01)\n        num_salt = int(prob * scene.size * 0.5)\n        coords_salt = (\n            np.random.randint(0, scene.shape[0], num_salt),\n            np.random.randint(0, scene.shape[1], num_salt),\n        )\n        scene[coords_salt] = 255\n        num_pepper = int(prob * scene.size * 0.5)\n        coords_pepper = (\n            np.random.randint(0, scene.shape[0], num_pepper),\n            np.random.randint(0, scene.shape[1], num_pepper),\n        )\n        scene[coords_pepper] = 0\n\n    # Add shadows\n    num_shadows = random.randint(0, 3)\n    for _ in range(num_shadows):\n        x1 = random.randint(0, img_w - 10)\n        y1 = random.randint(0, img_h - 10)\n        w = random.randint(10, min(img_w // 4, img_w - x1))\n        h = random.randint(10, min(img_h // 4, img_h - y1))\n        shadow_factor = random.uniform(0.3, 0.8)\n        scene[y1 : y1 + h, x1 : x1 + w] = (scene[y1 : y1 + h, x1 : x1 + w] * shadow_factor).astype(\n            np.uint8\n        )\n\n    # Add scratches\n    pil_scene = Image.fromarray(scene)\n    draw = ImageDraw.Draw(pil_scene)\n    num_lines = random.randint(0, 5)\n    for _ in range(num_lines):\n        x1 = random.randint(0, img_w)\n        y1 = random.randint(0, img_h)\n        x2 = random.randint(0, img_w)\n        y2 = random.randint(0, img_h)\n        width = random.randint(1, 3)\n        color = random.choice([0, 255])\n        draw.line([x1, y1, x2, y2], fill=color, width=width)\n    scene = np.array(pil_scene)\n\n    return scene, labels\n\n\n# ──────────────────────────────────────────────────────────────────────────────\n# 4.  ALBUMENTATIONS AUGMENTATION PIPELINE\n# ──────────────────────────────────────────────────────────────────────────────\n\n\ndef build_augmentation_pipeline(train: bool = True) -> A.Compose:\n    \"\"\"\n    Returns an albumentations Compose pipeline that is bounding-box-aware.\n    Bounding boxes are expected in 'albumentations' format: [x_min, y_min, x_max, y_max]\n    all normalised to [0, 1].\n    \"\"\"\n    bbox_params = A.BboxParams(\n        format=\"albumentations\",  # normalised [x1,y1,x2,y2]\n        label_fields=[\"class_labels\"],\n        min_area=0.0005,  # drop tiny boxes after crop\n        min_visibility=0.7,  # drop boxes occluded >30%\n    )\n\n    if not train:\n        # Validation: only normalize pixel values, no geometric changes\n        return A.Compose([A.NoOp()], bbox_params=bbox_params)\n\n    return A.Compose(\n        [\n            # ── Geometric ──────────────────────────────────────────────────\n            A.HorizontalFlip(p=0.5),\n            A.VerticalFlip(p=0.2),\n            A.Rotate(limit=10, border_mode=cv2.BORDER_REFLECT_101, p=0.4),\n            A.ShiftScaleRotate(\n                shift_limit=0.05,\n                scale_limit=0.15,\n                rotate_limit=8,\n                border_mode=cv2.BORDER_REFLECT_101,\n                p=0.4,\n            ),\n            A.Perspective(scale=(0.02, 0.06), p=0.3),\n            A.RandomResizedCrop(\n                size=(CFG.img_h, CFG.img_w),\n                scale=(0.75, 1.0),\n                ratio=(CFG.img_w / CFG.img_h * 0.9, CFG.img_w / CFG.img_h * 1.1),\n                p=0.3,\n            ),\n            # ── Photometric ────────────────────────────────────────────────\n            A.RandomBrightnessContrast(brightness_limit=0.3, contrast_limit=0.3, p=0.6),\n            A.GaussNoise(std_range=(0.05, 0.3), p=0.4),\n            A.GaussianBlur(blur_limit=(3, 7), p=0.3),\n            A.MotionBlur(blur_limit=7, p=0.2),\n            A.ImageCompression(quality_range=(40, 95), p=0.3),\n            A.RandomGamma(gamma_limit=(70, 130), p=0.3),\n            A.Sharpen(alpha=(0.1, 0.4), lightness=(0.8, 1.2), p=0.2),\n            # ── Occlusion ──────────────────────────────────────────────────\n            A.CoarseDropout(\n                num_holes_range=(1, 6),\n                hole_height_range=(10, 60),\n                hole_width_range=(10, 60),\n                fill=0,\n                p=0.3,\n            ),\n        ],\n        bbox_params=bbox_params,\n    )\n\n# ──────────────────────────────────────────────────────────────────────────────\n# 5.  DATASET WRITER\n# ──────────────────────────────────────────────────────────────────────────────\n\n\ndef generate_split(\n    split: str,\n    n: int,\n    out_dir: Path,\n    bg_dir: Path = None,\n    img_w: int = CFG.img_w,\n    img_h: int = CFG.img_h,\n):\n    \"\"\"Generate `n` labelled images into `out_dir/split/{images,labels.json}`.\"\"\"\n    img_dir = out_dir / split / \"images\"\n    img_dir.mkdir(parents=True, exist_ok=True)\n    annotations = {}\n\n    for i in range(n):\n        scene, labels = compose_scene(img_w, img_h, bg_dir=bg_dir)\n        fname = f\"{uuid.uuid4().hex}.png\"\n        Image.fromarray(scene).save(img_dir / fname)\n        annotations[fname] = labels\n        if (i + 1) % 100 == 0:\n            print(f\"  [{split}] {i + 1}/{n} generated\")\n\n    with open(out_dir / split / \"labels.json\", \"w\") as f:\n        json.dump(annotations, f, indent=2)\n    print(f\"  [{split}] Done. Saved {n} images + labels.json\")\n\n\ndef visualise_sample(out_path: str = \"sample_preview.png\", n: int = 4):\n    \"\"\"\n    Quick visual sanity-check: generate `n` scenes and tile them with\n    bounding-box overlays drawn on top.\n    \"\"\"\n    cols = 2\n    rows = math.ceil(n / cols)\n    tile_w, tile_h = CFG.img_w // 2, CFG.img_h // 2\n    canvas = Image.new(\"L\", (cols * tile_w, rows * tile_h), color=128)\n\n    for idx in range(n):\n        scene, labels = compose_scene()\n        pil = Image.fromarray(scene).resize((tile_w, tile_h))\n        draw = ImageDraw.Draw(pil)\n        for lb in labels:\n            x1 = int(lb[\"x1\"] * tile_w)\n            y1 = int(lb[\"y1\"] * tile_h)\n            x2 = int(lb[\"x2\"] * tile_w)\n            y2 = int(lb[\"y2\"] * tile_h)\n            draw.rectangle([x1, y1, x2, y2], outline=255, width=2)\n            draw.rectangle([x1 + 2, y1 + 2, x2 - 2, y2 - 2], outline=80, width=1)\n        col, row = idx % cols, idx // cols\n        canvas.paste(pil, (col * tile_w, row * tile_h))\n\n    canvas.save(out_path)\n    print(f\"Preview saved -> {out_path}\")\n\nprint(\"Dataset Generation Functions defined.\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.328486Z","iopub.execute_input":"2026-04-11T07:06:41.328878Z","iopub.status.idle":"2026-04-11T07:06:41.408981Z","shell.execute_reply.started":"2026-04-11T07:06:41.328853Z","shell.execute_reply":"2026-04-11T07:06:41.408270Z"}},"outputs":[{"name":"stdout","text":"Dataset Generation Functions defined.\n","output_type":"stream"}],"execution_count":14},{"cell_type":"code","source":"# 4) Dataset API (QRDataset) and loader builder (simplified for notebook)\n# dataset.py\n# ─────────────────────────────────────────────────────────────────────────────\n# PyTorch Dataset and DataLoader factories for the QR detection task.\n# ─────────────────────────────────────────────────────────────────────────────\n\nimport json\nfrom pathlib import Path\nfrom typing import Dict, List, Tuple\n\nimport numpy as np\nimport torch\nfrom PIL import Image\nfrom torch.utils.data import DataLoader, Dataset\n\nclass QRDataset(Dataset):\n    \"\"\"\n    Loads pre-generated scenes from disk.\n\n    Each sample returns:\n      image  : FloatTensor (1, H, W)  values in [0, 1]\n      boxes  : FloatTensor (N, 4)     [x1, y1, x2, y2] normalised\n      n_obj  : int                    actual number of QR codes in this image\n    \"\"\"\n\n    def __init__(self, split: str = \"train\", cfg=CFG):\n        self.cfg = cfg\n        self.split = split\n        self.img_dir = cfg.dataset_dir / split / \"images\"\n        label_file = cfg.dataset_dir / split / \"labels.json\"\n\n        with open(label_file) as f:\n            raw = json.load(f)\n\n        # Convert to list of (filename, list_of_boxes)\n        self.samples: List[Tuple[str, List[Dict]]] = [\n            (fname, boxes) for fname, boxes in raw.items()\n        ]\n        self.augment = build_augmentation_pipeline(train=(split == \"train\"))\n\n    def __len__(self) -> int:\n        return len(self.samples)\n\n    def __getitem__(self, idx: int):\n        fname, box_dicts = self.samples[idx]\n        img = np.array(Image.open(self.img_dir / fname).convert(\"L\"))  # (H, W)\n\n        # Convert box dicts → list of [x1, y1, x2, y2]\n        bboxes = [[b[\"x1\"], b[\"y1\"], b[\"x2\"], b[\"y2\"]] for b in box_dicts]\n        class_labels = [0] * len(bboxes)  # single class: \"qr_code\"\n\n        # Apply albumentations (bbox-aware)\n        result = self.augment(\n            image=img,\n            bboxes=bboxes,\n            class_labels=class_labels,\n        )\n        img_aug = result[\"image\"]           # (H, W) uint8\n        bboxes_aug = result[\"bboxes\"]       # list of (x1,y1,x2,y2) tuples\n\n        # To tensor\n        img_tensor = torch.from_numpy(img_aug).float().unsqueeze(0) / 255.0  # (1,H,W)\n\n        n_obj = len(bboxes_aug)\n        if n_obj > 0:\n            boxes_tensor = torch.tensor(bboxes_aug, dtype=torch.float32)  # (n_obj, 4)\n        else:\n            boxes_tensor = torch.zeros((0, 4), dtype=torch.float32)\n\n        return img_tensor, boxes_tensor, n_obj\n\n\ndef collate_fn(batch):\n    \"\"\"\n    Custom collate: images are stacked, boxes kept as a list of tensors\n    (variable length per image).\n    \"\"\"\n    images, boxes_list, n_objs = zip(*batch)\n    images = torch.stack(images, dim=0)  # (B, 1, H, W)\n    # boxes_list: tuple of FloatTensor (n_i, 4)\n    return images, list(boxes_list), list(n_objs)\n\n\ndef build_dataloaders(cfg=CFG):\n    train_ds = QRDataset(\"train\", cfg)\n    val_ds   = QRDataset(\"val\",   cfg)\n    test_ds  = QRDataset(\"test\",  cfg)\n\n    train_dl = DataLoader(\n        train_ds,\n        batch_size=cfg.batch_size,\n        shuffle=True,\n        num_workers=cfg.num_workers,\n        collate_fn=collate_fn,\n        pin_memory=True,\n        drop_last=True,\n    )\n    val_dl = DataLoader(\n        val_ds,\n        batch_size=cfg.batch_size,\n        shuffle=False,\n        num_workers=cfg.num_workers,\n        collate_fn=collate_fn,\n        pin_memory=True,\n    )\n    test_dl = DataLoader(\n        test_ds,\n        batch_size=1,\n        shuffle=False,\n        num_workers=0,\n        collate_fn=collate_fn,\n    )\n    return train_dl, val_dl, test_dl\n\nprint(\"Dataset Generator setup.\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.410425Z","iopub.execute_input":"2026-04-11T07:06:41.410740Z","iopub.status.idle":"2026-04-11T07:06:41.428259Z","shell.execute_reply.started":"2026-04-11T07:06:41.410704Z","shell.execute_reply":"2026-04-11T07:06:41.427447Z"}},"outputs":[{"name":"stdout","text":"Dataset Generator setup.\n","output_type":"stream"}],"execution_count":15},{"cell_type":"code","source":"# 5) DETR-like loss and matching helpers (hungarian, giou, set criterion)\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\nimport numpy as np\n\n# ──────────────────────────────────────────────────────────────────────────────\n# LOSS\n# ──────────────────────────────────────────────────────────────────────────────\n\ndef box_cxcywh_to_xyxy(boxes: torch.Tensor) -> torch.Tensor:\n    \"\"\"Convert [cx, cy, w, h] → [x1, y1, x2, y2]. No-op here since we already use xyxy.\"\"\"\n    return boxes\n\n\ndef generalized_iou(boxes1: torch.Tensor, boxes2: torch.Tensor) -> torch.Tensor:\n    \"\"\"\n    Compute pairwise GIoU between two sets of boxes.\n    Both inputs: (N, 4) in [x1, y1, x2, y2] format, values in [0, 1].\n    Returns: (N, M) GIoU matrix.\n    \"\"\"\n    N, M = len(boxes1), len(boxes2)\n    x1 = boxes1[:, 0].unsqueeze(1).expand(N, M)\n    y1 = boxes1[:, 1].unsqueeze(1).expand(N, M)\n    x2 = boxes1[:, 2].unsqueeze(1).expand(N, M)\n    y2 = boxes1[:, 3].unsqueeze(1).expand(N, M)\n\n    gx1 = boxes2[:, 0].unsqueeze(0).expand(N, M)\n    gy1 = boxes2[:, 1].unsqueeze(0).expand(N, M)\n    gx2 = boxes2[:, 2].unsqueeze(0).expand(N, M)\n    gy2 = boxes2[:, 3].unsqueeze(0).expand(N, M)\n\n    inter_x1 = torch.max(x1, gx1)\n    inter_y1 = torch.max(y1, gy1)\n    inter_x2 = torch.min(x2, gx2)\n    inter_y2 = torch.min(y2, gy2)\n\n    inter_w = (inter_x2 - inter_x1).clamp(min=0)\n    inter_h = (inter_y2 - inter_y1).clamp(min=0)\n    inter_area = inter_w * inter_h\n\n    area1 = (x2 - x1).clamp(min=0) * (y2 - y1).clamp(min=0)\n    area2 = (gx2 - gx1).clamp(min=0) * (gy2 - gy1).clamp(min=0)\n    union_area = area1 + area2 - inter_area + 1e-6\n\n    iou = inter_area / union_area\n\n    # Enclosing box\n    enc_x1 = torch.min(x1, gx1)\n    enc_y1 = torch.min(y1, gy1)\n    enc_x2 = torch.max(x2, gx2)\n    enc_y2 = torch.max(y2, gy2)\n    enc_area = ((enc_x2 - enc_x1).clamp(min=0) * (enc_y2 - enc_y1).clamp(min=0)).clamp(min=1e-6)\n\n    giou = iou - (enc_area - union_area) / enc_area\n    return giou  # (N, M)\n\n\ndef hungarian_match(\n    pred_boxes: torch.Tensor,   # (Q, 4)\n    pred_logits: torch.Tensor,  # (Q, 2)\n    gt_boxes: torch.Tensor,     # (G, 4)\n    cfg=CFG,\n) -> Tuple[List[int], List[int]]:\n    \"\"\"\n    Compute the optimal bipartite assignment between Q predictions and G ground-truths.\n    Returns (pred_indices, gt_indices) after Hungarian matching.\n    \"\"\"\n    Q, G = len(pred_boxes), len(gt_boxes)\n    if G == 0:\n        return [], []\n\n    with torch.no_grad():\n        # Class cost: negative probability of being a QR code\n        prob = pred_logits.softmax(-1)          # (Q, 2)\n        cost_class = -prob[:, 0].unsqueeze(1).expand(Q, G)   # (Q, G)\n\n        # L1 cost\n        cost_l1 = torch.cdist(pred_boxes, gt_boxes, p=1)     # (Q, G)\n\n        # GIoU cost\n        giou = generalized_iou(pred_boxes, gt_boxes)          # (Q, G)\n        cost_giou = -giou\n\n        cost = (\n            cfg.cost_class * cost_class\n            + cfg.cost_bbox * cost_l1\n            + cfg.cost_giou * cost_giou\n        )\n        cost_np = cost.cpu().numpy()\n\n    row_ind, col_ind = linear_sum_assignment(cost_np)\n    return row_ind.tolist(), col_ind.tolist()\n\n\nclass SetCriterion(nn.Module):\n    \"\"\"\n    DETR-style set prediction loss.\n    For each image: run Hungarian matching, then compute\n      - Cross-entropy for classification (incl. no-object class)\n      - L1 + GIoU for matched box pairs\n    \"\"\"\n\n    def __init__(self, cfg=CFG):\n        super().__init__()\n        self.cfg = cfg\n        # Down-weight the \"no-object\" class to handle the class imbalance\n        # between num_queries (many) and actual QR codes (few)\n        eos_weight = torch.ones(2)\n        eos_weight[1] = cfg.eos_coef   # index 1 = \"no-object\"\n        self.register_buffer(\"eos_weight\", eos_weight)\n\n    def forward(\n        self,\n        pred_boxes_batch: torch.Tensor,    # (B, Q, 4)\n        pred_logits_batch: torch.Tensor,   # (B, Q, 2)\n        gt_boxes_list: List[torch.Tensor], # list of (G_i, 4)\n    ) -> dict:\n        B, Q, _ = pred_boxes_batch.shape\n        device = pred_boxes_batch.device\n\n        total_loss_class = torch.tensor(0.0, device=device)\n        total_loss_bbox  = torch.tensor(0.0, device=device)\n        total_loss_giou  = torch.tensor(0.0, device=device)\n        num_boxes = 0\n\n        for b in range(B):\n            pred_b = pred_boxes_batch[b]    # (Q, 4)\n            logit_b = pred_logits_batch[b]  # (Q, 2)\n            gt_b = gt_boxes_list[b].to(device)  # (G, 4)\n            G = len(gt_b)\n\n            # ── Classification loss for ALL queries ────────────────────────\n            # Target: \"no-object\" (1) for all, overwrite matched with 0\n            tgt_labels = torch.ones(Q, dtype=torch.long, device=device)  # all no-obj\n\n            if G > 0:\n                pred_idx, gt_idx = hungarian_match(pred_b, logit_b, gt_b, self.cfg)\n                tgt_labels[pred_idx] = 0   # matched queries → \"qr_code\" class\n\n                # ── Box losses only for matched pairs ──────────────────────\n                matched_pred = pred_b[pred_idx]    # (|match|, 4)\n                matched_gt   = gt_b[gt_idx]         # (|match|, 4)\n                n_match = len(pred_idx)\n\n                # L1\n                loss_l1 = F.l1_loss(matched_pred, matched_gt, reduction=\"sum\")\n\n                # GIoU\n                giou_mat = generalized_iou(matched_pred, matched_gt)\n                loss_giou = (1 - giou_mat.diag()).sum()\n\n                total_loss_bbox += loss_l1\n                total_loss_giou += loss_giou\n                num_boxes += n_match\n\n            # Cross-entropy (weighted for no-object imbalance)\n            total_loss_class += F.cross_entropy(logit_b, tgt_labels, weight=self.eos_weight)\n\n        # Normalise box losses by total matched boxes across batch\n        norm = max(num_boxes, 1)\n        loss_dict = {\n            \"loss_class\": total_loss_class / B,\n            \"loss_bbox\":  total_loss_bbox  / norm,\n            \"loss_giou\":  total_loss_giou  / norm,\n        }\n        total = (\n            self.cfg.loss_class * loss_dict[\"loss_class\"]\n            + self.cfg.loss_bbox  * loss_dict[\"loss_bbox\"]\n            + self.cfg.loss_giou  * loss_dict[\"loss_giou\"]\n        )\n        loss_dict[\"total\"] = total\n        return loss_dict\n\nprint(\"Additional measurements defined.\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.429341Z","iopub.execute_input":"2026-04-11T07:06:41.429714Z","iopub.status.idle":"2026-04-11T07:06:41.449430Z","shell.execute_reply.started":"2026-04-11T07:06:41.429677Z","shell.execute_reply":"2026-04-11T07:06:41.448628Z"}},"outputs":[{"name":"stdout","text":"Additional measurements defined.\n","output_type":"stream"}],"execution_count":16},{"cell_type":"code","source":"# 6) Evaluation metrics (precision/recall/F1, mean IoU) and helper IOU\nimport numpy as np\nfrom typing import List\n\n# ──────────────────────────────────────────────────────────────────────────────\n# METRICS\n# ──────────────────────────────────────────────────────────────────────────────\n\ndef iou_single(b1: np.ndarray, b2: np.ndarray) -> float:\n    \"\"\"IoU between two boxes [x1,y1,x2,y2].\"\"\"\n    ix1 = max(b1[0], b2[0]); iy1 = max(b1[1], b2[1])\n    ix2 = min(b1[2], b2[2]); iy2 = min(b1[3], b2[3])\n    inter = max(0, ix2 - ix1) * max(0, iy2 - iy1)\n    a1 = max(0, b1[2] - b1[0]) * max(0, b1[3] - b1[1])\n    a2 = max(0, b2[2] - b2[0]) * max(0, b2[3] - b2[1])\n    union = a1 + a2 - inter\n    return inter / union if union > 0 else 0.0\n\n\n@torch.no_grad()\ndef compute_map(\n    model: nn.Module,\n    dataloader,\n    cfg=CFG,\n    device: torch.device = torch.device(\"cpu\"),\n) -> dict:\n    \"\"\"\n    Compute Precision, Recall, and mAP@0.5 over the dataloader.\n    \"\"\"\n    model.eval()\n    all_tp = all_fp = all_fn = 0\n    total_iou = 0.0\n    total_gt = 0\n\n    for images, gt_boxes_list, _ in tqdm(dataloader, desc=\"Eval\", leave=False):\n        images = images.to(device)\n        out = model(images)\n        pred_boxes_batch  = out[\"pred_boxes\"]   # (B, Q, 4)\n        pred_logits_batch = out[\"pred_logits\"]  # (B, Q, 2)\n\n        B = images.size(0)\n        for b in range(B):\n            # Filter predictions by confidence\n            probs = pred_logits_batch[b].softmax(-1)[:, 0]  # P(qr_code)\n            keep  = probs >= cfg.conf_threshold\n            pred_b = pred_boxes_batch[b][keep].cpu().numpy()\n\n            gt_b = gt_boxes_list[b].numpy()\n            G = len(gt_b)\n            P = len(pred_b)\n            total_gt += G\n\n            if G == 0 and P == 0:\n                continue\n            if G == 0:\n                all_fp += P\n                continue\n            if P == 0:\n                all_fn += G\n                continue\n\n            # Greedy match by IoU\n            matched_gt = set()\n            for pred in pred_b:\n                best_iou, best_j = 0.0, -1\n                for j, gt in enumerate(gt_b):\n                    if j in matched_gt:\n                        continue\n                    iou = iou_single(pred, gt)\n                    if iou > best_iou:\n                        best_iou, best_j = iou, j\n                if best_iou >= cfg.iou_threshold:\n                    all_tp += 1\n                    matched_gt.add(best_j)\n                    total_iou += best_iou\n                else:\n                    all_fp += 1\n            all_fn += G - len(matched_gt)\n\n    eps = 1e-6\n    precision = all_tp / (all_tp + all_fp + eps)\n    recall    = all_tp / (all_tp + all_fn + eps)\n    f1        = 2 * precision * recall / (precision + recall + eps)\n    mean_iou  = total_iou / (all_tp + eps)\n\n    return {\n        \"precision\": precision,\n        \"recall\":    recall,\n        \"f1\":        f1,\n        \"mean_iou\":  mean_iou,\n        \"tp\": all_tp, \"fp\": all_fp, \"fn\": all_fn,\n    }\n\ndef iou_between_sets(boxes1: List[np.ndarray], boxes2: List[np.ndarray]) -> float:\n    if len(boxes1) == 0 or len(boxes2) == 0:\n        return 0.0\n    best = 0.0\n    for b1 in boxes1:\n        for b2 in boxes2:\n            best = max(best, iou_single(b1, b2))\n    return best\n\nprint(\"Metrics Defined.\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.450992Z","iopub.execute_input":"2026-04-11T07:06:41.451518Z","iopub.status.idle":"2026-04-11T07:06:41.468020Z","shell.execute_reply.started":"2026-04-11T07:06:41.451485Z","shell.execute_reply":"2026-04-11T07:06:41.467224Z"}},"outputs":[{"name":"stdout","text":"Metrics Defined.\n","output_type":"stream"}],"execution_count":17},{"cell_type":"code","source":"# 7) Training loop (simplified) - note: this is for notebook demonstration. For full training, use the train script.\n\"\"\"device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')\nfrom model import QRViTDet\nfrom train_eval import SetCriterion, compute_map # will import our notebook's version if available\n\nmodel = QRViTDet(CFG).to(device)\n\nparam_groups = [\n {\"params\": model.backbone_params, \"lr\": CFG.lr_backbone},\n {\"params\": model.non_backbone_params, \"lr\": CFG.lr},\n]\noptimizer = torch.optim.AdamW(param_groups, weight_decay=CFG.weight_decay)\nscheduler = torch.optim.lr_scheduler.StepLR(optimizer, step_size=CFG.lr_drop, gamma=0.1)\ncriterion = SetCriterion(CFG).to(device)\n\n# Create tiny dataset for demonstration (generate or reuse existing)\ntrain_dl, val_dl, test_dl = build_dataloaders(CFG)\nprint('Dataloaders ready: train', len(train_dl), 'val', len(val_dl))\n\ndef train_one_epoch(epoch_id, max_batches: int = 3):\n model.train()\n running = {\"total\": 0.0, \"loss_class\": 0.0, \"loss_bbox\": 0.0, \"loss_giou\": 0.0}\n t0 = time.time()\n for batch_idx, (images, gt_boxes_list, ) in enumerate(train_dl):\n if batch_idx >= max_batches:\n break\n images = images.to(device)\n out = model(images)\n loss_dict = criterion(out[\"pred_boxes\"], out[\"pred_logits\"], gt_boxes_list)\n loss = loss_dict[\"total\"]\n optimizer.zero_grad()\n loss.backward()\n if CFG.grad_clip > 0:\n nn.utils.clip_grad_norm(model.parameters(), CFG.grad_clip)\n optimizer.step()\n for k in running:\n running[k] += loss_dict[k].item()\n scheduler.step()\n avg = running['total'] / max(1, max_batches)\n print(f\"Epoch {epoch_id} train avg loss ~ {avg:.4f} (batches: {max_batches}) time={time.time()-t0:.1f}s\")\n\nfor e in range(1, 3):\n train_one_epoch(e, max_batches=3)\n metrics = compute_map(model, val_dl, CFG, device)\n print('Validation metrics after epoch', e, metrics)\n # Save a tiny checkpoint\n CFG.checkpoint_dir.mkdir(parents=True, exist_ok=True)\n ckpt_path = CFG.checkpoint_dir / f'notebook_epoch{e}.pt'\n torch.save({\"epoch\": e, \"model_state\": model.state_dict(), \"cfg\": CFG}, ckpt_path)\n print('Checkpoint saved ->', ckpt_path)\"\"\"\nimport os\nimport argparse\nimport time\nfrom pathlib import Path\nfrom typing import List, Tuple\nimport torch.nn.functional as F\nfrom PIL import Image, ImageDraw\nfrom scipy.optimize import linear_sum_assignment\nfrom torch.optim import AdamW\nfrom torch.optim.lr_scheduler import StepLR\nfrom tqdm import tqdm\n\n# ──────────────────────────────────────────────────────────────────────────────\n# TRAINING\n# ──────────────────────────────────────────────────────────────────────────────\n\ndef train(cfg=CFG):\n    try:\n        device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n    except:\n        !pip install -U pip\n        !pip install -U torch torchvision torchaudio\n        device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n        \n    print(f\"Device: {device}\")\n\n    cfg.checkpoint_dir.mkdir(parents=True, exist_ok=True)\n\n    # ── Data ────────────────────────────────────────────────────────────────\n    train_dl, val_dl, _ = build_dataloaders(cfg)\n    print(f\"Train batches: {len(train_dl)} | Val batches: {len(val_dl)}\")\n\n    # ── Model ────────────────────────────────────────────────────────────────\n    model = QRViTDet(cfg).to(device)\n\n    # ── Optimiser: separate LR for backbone ─────────────────────────────────\n    param_groups = [\n        {\"params\": model.backbone_params,     \"lr\": cfg.lr_backbone},\n        {\"params\": model.non_backbone_params, \"lr\": cfg.lr},\n    ]\n    optimizer = AdamW(param_groups, weight_decay=cfg.weight_decay)\n    scheduler = StepLR(optimizer, step_size=cfg.lr_drop, gamma=0.1)\n    criterion = SetCriterion(cfg).to(device)\n\n    checkpoint_path = f\"{cfg.checkpoint_dir}/best.pt\"\n    if os.path.exists(checkpoint_path):\n        print(f\"Resuming from existing checkpoint: {checkpoint_path}\")\n        ckpt = torch.load(checkpoint_path, map_location=device, weights_only=False)\n        model.load_state_dict(ckpt[\"model_state\"])\n        optimizer.load_state_dict(ckpt[\"optimizer_state\"])\n        scheduler.load_state_dict(ckpt[\"scheduler_state\"])\n        start_epoch = ckpt[\"epoch\"] + 1\n        end_epoch = start_epoch + cfg.epochs + 1\n        best_f1 = ckpt[\"metrics\"][\"f1\"]\n        print(f\"Resumed at epoch {start_epoch}, current best F1: {best_f1:.4f}\")\n    else:\n        scheduler = StepLR(optimizer, step_size=cfg.lr_drop, gamma=0.1)\n        criterion = SetCriterion(cfg).to(device)\n        start_epoch = 1\n        end_epoch = cfg.epochs + 1\n        best_f1 = 0.0\n        print(\"No existing checkpoint found - starting fresh training\")\n        n_params = sum(p.numel() for p in model.parameters() if p.requires_grad)\n        print(f\"Trainable params: {n_params:,}\")\n\n    for epoch in range(start_epoch, end_epoch):\n        model.train()\n        running = {\"total\": 0.0, \"loss_class\": 0.0, \"loss_bbox\": 0.0, \"loss_giou\": 0.0}\n        t0 = time.time()\n        #print(\"Starting epoch {:03d}...\".format(epoch))\n\n        for batch_idx, (images, gt_boxes_list, _) in enumerate(train_dl):\n            images = images.to(device)\n\n            out = model(images)\n            loss_dict = criterion(out[\"pred_boxes\"], out[\"pred_logits\"], gt_boxes_list)\n            loss = loss_dict[\"total\"]\n            #print(f\"Batch {batch_idx+1}/{len(train_dl)}  Loss: {loss.item():.4f}\")\n\n            optimizer.zero_grad()\n            loss.backward()\n            if cfg.grad_clip > 0:\n                nn.utils.clip_grad_norm_(model.parameters(), cfg.grad_clip)\n            optimizer.step()\n\n            for k in running:\n                running[k] += loss_dict[k].item()\n\n            if (batch_idx + 1) % cfg.log_interval == 0:\n                n = cfg.log_interval\n                print(\n                    f\"Epoch {epoch:03d} [{batch_idx+1}/{len(train_dl)}] \"\n                    f\"loss={running['total']/n:.4f}  \"\n                    f\"cls={running['loss_class']/n:.4f}  \"\n                    f\"bbox={running['loss_bbox']/n:.4f}  \"\n                    f\"giou={running['loss_giou']/n:.4f}  \"\n                    f\"({time.time()-t0:.1f}s)\"\n                )\n                running = {k: 0.0 for k in running}\n                t0 = time.time()\n\n        scheduler.step()\n\n        # ── Validation ───────────────────────────────────────────────────────\n        metrics = compute_map(model, val_dl, cfg, device)\n        print(\n            f\"  [Val] Epoch {epoch:03d}  \"\n            f\"P={metrics['precision']:.4f}  R={metrics['recall']:.4f}  \"\n            f\"F1={metrics['f1']:.4f}  mIoU={metrics['mean_iou']:.4f}\"\n        )\n\n        # ── Checkpoint ───────────────────────────────────────────────────────\n        ckpt = {\n            \"epoch\": epoch,\n            \"model_state\": model.state_dict(),\n            \"optimizer_state\": optimizer.state_dict(),\n            \"scheduler_state\": scheduler.state_dict(),\n            \"metrics\": metrics,\n            \"cfg\": cfg,\n        }\n        torch.save(ckpt, cfg.checkpoint_dir / \"last.pt\")\n\n        if metrics[\"f1\"] > best_f1:\n            best_f1 = metrics[\"f1\"]\n            torch.save(ckpt, cfg.checkpoint_dir / \"best.pt\")\n            print(f\"  ★ New best F1: {best_f1:.4f} → saved best.pt\")\n\n    print(f\"\\nTraining complete. Best val F1 = {best_f1:.4f}\")\n\n\n# ──────────────────────────────────────────────────────────────────────────────\n# EVALUATION\n# ──────────────────────────────────────────────────────────────────────────────\n\ndef evaluate(checkpoint_path: str, cfg=CFG):\n    device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n\n    ckpt = torch.load(checkpoint_path, map_location=device, weights_only=False)\n    model = QRViTDet(cfg).to(device)\n    model.load_state_dict(ckpt[\"model_state\"])\n    print(f\"Loaded checkpoint from epoch {ckpt['epoch']}\")\n\n    _, _, test_dl = build_dataloaders(cfg)\n    metrics = compute_map(model, test_dl, cfg, device)\n\n    print(\"\\n── Test Set Metrics ────────────────────────\")\n    for k, v in metrics.items():\n        print(f\"  {k:>12s}: {v}\")\n    print(\"────────────────────────────────────────────\")\n    return metrics\n\n\n# ──────────────────────────────────────────────────────────────────────────────\n# INFERENCE\n# ──────────────────────────────────────────────────────────────────────────────\n\n@torch.no_grad()\ndef infer(\n    image_path: str,\n    checkpoint_path: str,\n    output_path: str = \"output.png\",\n    cfg=CFG,\n):\n    \"\"\"\n    Run inference on a single image.\n    Saves the input image with detected QR bounding boxes overlaid.\n    \"\"\"\n    device = torch.device(\"cuda\" if torch.cuda.is_available() else \"cpu\")\n\n    # Load model\n    ckpt = torch.load(checkpoint_path, map_location=device, weights_only=False)\n    model = QRViTDet(cfg).to(device)\n    model.load_state_dict(ckpt[\"model_state\"])\n    model.eval()\n\n    # Load & preprocess image\n    img = Image.open(image_path).convert(\"L\").resize((cfg.img_w, cfg.img_h))\n    img_np = np.array(img)\n    img_tensor = torch.from_numpy(img_np).float().unsqueeze(0).unsqueeze(0) / 255.0\n    img_tensor = img_tensor.to(device)  # (1, 1, H, W)\n\n    # Forward\n    out = model(img_tensor)\n    pred_boxes  = out[\"pred_boxes\"][0]   # (Q, 4)\n    pred_logits = out[\"pred_logits\"][0]  # (Q, 2)\n\n    # Filter by confidence\n    probs = pred_logits.softmax(-1)[:, 0]\n    keep  = probs >= cfg.conf_threshold\n    final_boxes = pred_boxes[keep].cpu().numpy()\n    final_confs = probs[keep].cpu().numpy()\n\n    # Visualise\n    W, H = img.size\n    pil_out = img.convert(\"RGB\")\n    draw = ImageDraw.Draw(pil_out)\n\n    print(f\"\\nDetected {len(final_boxes)} QR code(s):\")\n    for i, (box, conf) in enumerate(zip(final_boxes, final_confs)):\n        x1 = int(box[0] * W);  y1 = int(box[1] * H)\n        x2 = int(box[2] * W);  y2 = int(box[3] * H)\n        print(f\"  [{i+1}] TL=({x1},{y1})  BR=({x2},{y2})  conf={conf:.3f}\")\n        draw.rectangle([x1, y1, x2, y2], outline=(0, 255, 0), width=3)\n        draw.text((x1 + 4, y1 + 4), f\"{conf:.2f}\", fill=(255, 255, 0))\n\n    pil_out.save(output_path)\n    print(f\"Annotated image saved → {output_path}\")\n    return final_boxes, final_confs\n\nprint(\"Training is now ready!\")\n\n","metadata":{"_kg_hide-input":true,"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:09:47.815536Z","iopub.execute_input":"2026-04-11T07:09:47.816016Z","iopub.status.idle":"2026-04-11T07:09:47.859324Z","shell.execute_reply.started":"2026-04-11T07:09:47.815984Z","shell.execute_reply":"2026-04-11T07:09:47.858539Z"}},"outputs":[{"name":"stdout","text":"Training is now ready!\n","output_type":"stream"}],"execution_count":23},{"cell_type":"code","source":"# Build the dataset if it hasn't been built yet\nout_dir = CFG.dataset_dir#\"data/qr_dataset\"\nbg_dir = CFG.background_dir#\"data/backgrounds\"\nif 0:\n    print(f\"Generating dataset -> {out_dir}\")\n    generate_split(\"train\", CFG.num_train, out_dir, bg_dir)\n    generate_split(\"val\", CFG.num_val, out_dir, bg_dir)\n    generate_split(\"test\", CFG.num_test, out_dir, bg_dir)\n    print(\"Dataset generation complete.\")","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.634172Z","iopub.execute_input":"2026-04-11T07:06:41.634598Z","iopub.status.idle":"2026-04-11T07:06:41.650867Z","shell.execute_reply.started":"2026-04-11T07:06:41.634563Z","shell.execute_reply":"2026-04-11T07:06:41.650127Z"}},"outputs":[],"execution_count":19},{"cell_type":"code","source":"# Start Training\ntrain(CFG)","metadata":{"trusted":true,"_kg_hide-input":false,"execution":{"iopub.status.busy":"2026-04-11T07:09:54.413662Z","iopub.execute_input":"2026-04-11T07:09:54.414480Z"}},"outputs":[{"name":"stdout","text":"Device: cuda\nTrain batches: 80 | Val batches: 20\nNo existing checkpoint found - starting fresh training\nTrainable params: 2,028,486\n","output_type":"stream"},{"name":"stderr","text":"/usr/local/lib/python3.12/dist-packages/albumentations/core/validation.py:114: UserWarning: ShiftScaleRotate is a special case of Affine transform. Please use Affine transform instead.\n  original_init(self, **validated_kwargs)\n","output_type":"stream"},{"name":"stdout","text":"Epoch 001 [10/80] loss=5.0217  cls=0.5545  bbox=0.4243  giou=0.8956  (6.9s)\nEpoch 001 [20/80] loss=4.5563  cls=0.5418  bbox=0.3354  giou=0.8979  (5.3s)\nEpoch 001 [30/80] loss=4.2895  cls=0.5172  bbox=0.3111  giou=0.8497  (5.3s)\nEpoch 001 [40/80] loss=4.2552  cls=0.5201  bbox=0.3065  giou=0.8412  (5.5s)\nEpoch 001 [50/80] loss=4.2044  cls=0.5140  bbox=0.3006  giou=0.8368  (5.3s)\nEpoch 001 [60/80] loss=4.0985  cls=0.5179  bbox=0.2813  giou=0.8280  (5.3s)\nEpoch 001 [70/80] loss=3.9505  cls=0.5004  bbox=0.2705  giou=0.7985  (5.3s)\nEpoch 001 [80/80] loss=4.0031  cls=0.5199  bbox=0.2714  giou=0.8031  (5.2s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 001  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 002 [10/80] loss=3.9191  cls=0.5137  bbox=0.2663  giou=0.7801  (6.2s)\nEpoch 002 [20/80] loss=3.9145  cls=0.4950  bbox=0.2698  giou=0.7877  (5.3s)\nEpoch 002 [30/80] loss=3.9011  cls=0.5030  bbox=0.2629  giou=0.7903  (5.4s)\nEpoch 002 [40/80] loss=3.9840  cls=0.5179  bbox=0.2753  giou=0.7858  (5.5s)\nEpoch 002 [50/80] loss=3.8576  cls=0.5061  bbox=0.2579  giou=0.7779  (5.4s)\nEpoch 002 [60/80] loss=3.9538  cls=0.5110  bbox=0.2675  giou=0.7972  (5.5s)\nEpoch 002 [70/80] loss=3.8116  cls=0.5102  bbox=0.2565  giou=0.7543  (5.4s)\nEpoch 002 [80/80] loss=3.8587  cls=0.5095  bbox=0.2564  giou=0.7789  (5.4s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 002  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 003 [10/80] loss=3.8349  cls=0.5119  bbox=0.2537  giou=0.7713  (6.6s)\nEpoch 003 [20/80] loss=3.8373  cls=0.5038  bbox=0.2589  giou=0.7676  (5.6s)\nEpoch 003 [30/80] loss=3.8316  cls=0.5009  bbox=0.2565  giou=0.7735  (5.6s)\nEpoch 003 [40/80] loss=3.8049  cls=0.5105  bbox=0.2501  giou=0.7666  (5.7s)\nEpoch 003 [50/80] loss=3.8209  cls=0.5100  bbox=0.2566  giou=0.7590  (5.6s)\nEpoch 003 [60/80] loss=3.8540  cls=0.5119  bbox=0.2552  giou=0.7771  (5.7s)\nEpoch 003 [70/80] loss=3.7644  cls=0.5034  bbox=0.2515  giou=0.7500  (5.8s)\nEpoch 003 [80/80] loss=3.7673  cls=0.5029  bbox=0.2470  giou=0.7633  (5.7s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 003  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 004 [10/80] loss=3.8199  cls=0.5058  bbox=0.2547  giou=0.7674  (6.4s)\nEpoch 004 [20/80] loss=3.7755  cls=0.5024  bbox=0.2489  giou=0.7631  (5.8s)\nEpoch 004 [30/80] loss=3.7892  cls=0.4992  bbox=0.2518  giou=0.7659  (5.7s)\nEpoch 004 [40/80] loss=3.7547  cls=0.5059  bbox=0.2462  giou=0.7559  (5.8s)\nEpoch 004 [50/80] loss=3.7421  cls=0.5022  bbox=0.2477  giou=0.7495  (5.7s)\nEpoch 004 [60/80] loss=3.8135  cls=0.5141  bbox=0.2524  giou=0.7617  (5.6s)\nEpoch 004 [70/80] loss=3.7530  cls=0.4944  bbox=0.2505  giou=0.7557  (5.7s)\nEpoch 004 [80/80] loss=3.8147  cls=0.5175  bbox=0.2532  giou=0.7570  (5.5s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 004  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 005 [10/80] loss=3.7580  cls=0.5048  bbox=0.2467  giou=0.7574  (6.2s)\nEpoch 005 [20/80] loss=3.7447  cls=0.5064  bbox=0.2448  giou=0.7541  (5.6s)\nEpoch 005 [30/80] loss=3.7213  cls=0.4939  bbox=0.2511  giou=0.7390  (5.7s)\nEpoch 005 [40/80] loss=3.7374  cls=0.5023  bbox=0.2493  giou=0.7431  (5.7s)\nEpoch 005 [50/80] loss=3.8043  cls=0.5094  bbox=0.2516  giou=0.7636  (5.7s)\nEpoch 005 [60/80] loss=3.7929  cls=0.5064  bbox=0.2528  giou=0.7581  (5.8s)\nEpoch 005 [70/80] loss=3.7573  cls=0.5028  bbox=0.2484  giou=0.7548  (5.7s)\nEpoch 005 [80/80] loss=3.7185  cls=0.5148  bbox=0.2405  giou=0.7433  (5.5s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 005  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 006 [10/80] loss=3.7200  cls=0.5006  bbox=0.2457  giou=0.7452  (6.4s)\nEpoch 006 [20/80] loss=3.7781  cls=0.5013  bbox=0.2520  giou=0.7579  (5.6s)\nEpoch 006 [30/80] loss=3.7400  cls=0.5089  bbox=0.2449  giou=0.7489  (5.5s)\nEpoch 006 [40/80] loss=3.7437  cls=0.5118  bbox=0.2454  giou=0.7465  (5.7s)\nEpoch 006 [50/80] loss=3.7161  cls=0.4949  bbox=0.2477  giou=0.7438  (5.8s)\nEpoch 006 [60/80] loss=3.6951  cls=0.5055  bbox=0.2424  giou=0.7361  (5.6s)\nEpoch 006 [70/80] loss=3.7686  cls=0.4956  bbox=0.2498  giou=0.7643  (5.7s)\nEpoch 006 [80/80] loss=3.7906  cls=0.5103  bbox=0.2494  giou=0.7615  (5.5s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 006  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 007 [10/80] loss=3.8013  cls=0.5118  bbox=0.2532  giou=0.7559  (6.4s)\nEpoch 007 [20/80] loss=3.8098  cls=0.5077  bbox=0.2519  giou=0.7674  (5.7s)\nEpoch 007 [30/80] loss=3.7574  cls=0.5043  bbox=0.2516  giou=0.7453  (5.6s)\nEpoch 007 [40/80] loss=3.7486  cls=0.5016  bbox=0.2508  giou=0.7457  (5.7s)\nEpoch 007 [50/80] loss=3.7379  cls=0.5035  bbox=0.2435  giou=0.7567  (5.7s)\nEpoch 007 [60/80] loss=3.7776  cls=0.5053  bbox=0.2538  giou=0.7491  (5.9s)\nEpoch 007 [70/80] loss=3.7503  cls=0.5034  bbox=0.2485  giou=0.7505  (5.7s)\nEpoch 007 [80/80] loss=3.7603  cls=0.5070  bbox=0.2454  giou=0.7597  (5.6s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 007  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 008 [10/80] loss=3.7069  cls=0.4983  bbox=0.2455  giou=0.7415  (6.4s)\nEpoch 008 [20/80] loss=3.7451  cls=0.5105  bbox=0.2466  giou=0.7455  (6.1s)\nEpoch 008 [30/80] loss=3.7266  cls=0.5014  bbox=0.2476  giou=0.7429  (5.7s)\nEpoch 008 [40/80] loss=3.7980  cls=0.5069  bbox=0.2500  giou=0.7671  (5.8s)\nEpoch 008 [50/80] loss=3.7376  cls=0.4958  bbox=0.2471  giou=0.7552  (5.7s)\nEpoch 008 [60/80] loss=3.7543  cls=0.5038  bbox=0.2482  giou=0.7529  (5.8s)\nEpoch 008 [70/80] loss=3.7310  cls=0.4963  bbox=0.2462  giou=0.7537  (5.7s)\nEpoch 008 [80/80] loss=3.7555  cls=0.5089  bbox=0.2459  giou=0.7540  (5.6s)\n","output_type":"stream"},{"name":"stderr","text":"                                                     \r","output_type":"stream"},{"name":"stdout","text":"  [Val] Epoch 008  P=0.0000  R=0.0000  F1=0.0000  mIoU=0.0000\nEpoch 009 [10/80] loss=3.7748  cls=0.5068  bbox=0.2504  giou=0.7547  (6.5s)\nEpoch 009 [20/80] loss=3.7132  cls=0.4995  bbox=0.2460  giou=0.7422  (5.8s)\nEpoch 009 [30/80] loss=3.7260  cls=0.4959  bbox=0.2484  giou=0.7461  (5.7s)\nEpoch 009 [40/80] loss=3.7608  cls=0.5128  bbox=0.2476  giou=0.7485  (5.7s)\nEpoch 009 [50/80] loss=3.7454  cls=0.4978  bbox=0.2465  giou=0.7586  (5.7s)\n","output_type":"stream"}],"execution_count":null},{"cell_type":"code","source":"# 8) Inference on a single image (demo)\ndef run_inference_on_dummy():\n # Create a blank image (or optionally load a sample) and run forward\n blank = torch.zeros((1, 1, CFG.img_h, CFG.img_w)).to(device)\n out = model(blank)\n pred_boxes = out[\"pred_boxes\"][0].detach().cpu().numpy()\n pred_logits = out[\"pred_logits\"][0].detach()\n confs = pred_logits.softmax(-1)[:, 0].detach().cpu().numpy()\n print('Pred boxes shape:', pred_boxes.shape)\n print('Top-5 confidences:', confs[:5])\n\nrun_inference_on_dummy()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.966337Z","iopub.status.idle":"2026-04-11T07:06:41.966639Z","shell.execute_reply.started":"2026-04-11T07:06:41.966513Z","shell.execute_reply":"2026-04-11T07:06:41.966528Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"# 9) Optional TorchScript export (for inference in non-Python environments)\ndef export_scripted_model():\n example_input = torch.randn(1, 1, CFG.img_h, CFG.img_w).to(device)\n try:\n scripted = torch.jit.trace(model, example_input)\n path = CFG.checkpoint_dir / 'qrvitdet_traced.pt'\n scripted.save(str(path))\n print('Exported TorchScript to', path)\n except Exception as e:\n print('TorchScript export failed:', e)\n\nexport_scripted_model()","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-04-11T07:06:41.968830Z","iopub.status.idle":"2026-04-11T07:06:41.969258Z","shell.execute_reply.started":"2026-04-11T07:06:41.969056Z","shell.execute_reply":"2026-04-11T07:06:41.969078Z"}},"outputs":[],"execution_count":null},{"cell_type":"markdown","source":"Notes and guidance\n- This notebook merges the repository's core components into executable cells. It uses a lightweight training loop and a tiny dataset setup for demonstration. For full-scale training, replace the dataset generation with your full data, and run the standard train_eval.py flow.\n- The dataset generation helper in this notebook is simplified and optional. If you already have a dataset on disk at data/qr_dataset, you can skip generation and directly train. Ensure the folder structure matches dataset_gen.py expectations: data/qr_dataset/{train,val,test}/images and a labels.json per split.\n- The code relies on a number of optional dependencies (albumentations, qrcode, cv2). Install them in your environment if you intend to run dataset generation or augmentation.\n- Inference and export steps are provided as convenience features in the notebook.","metadata":{}}]}